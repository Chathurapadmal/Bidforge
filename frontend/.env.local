NEXT_PUBLIC_API_BASE_URL=http://localhost:5014

# Next dev over HTTPS is fine; the browser calls https://localhost:3000  / 5014
# but your code will fetch http://localhost:5184 (different origin, allowed with CORS)


DATABASE_URL="Server=localhost,1433;Database=BidforgeDb;User Id=chathura;Password=7895123;TrustServerCertificate=True;"
